{
  "name": "avia",
  "private": true,
  "version": "0.0.0",
  "description": "AVIA — Dynamics 365 F&O AI agents (orchestrator + specialized agents + MCP servers).",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "clean": "pnpm -r exec rm -rf dist *.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.9.0",
    "vitest": "^2.1.4"
  }
}
